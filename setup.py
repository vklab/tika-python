from setuptools import setup, find_packages

setup(
    name='vklabs.tika-python',
    version='0.0.2',
    python_requires='>=3.6',
    packages=find_packages(),
    package_data={},
    install_requires=[
        'setuptools',
        'requests'
    ],
    entry_points='''
    ''',
)
